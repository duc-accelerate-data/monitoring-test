version: 2

sources:
  - name: raw
    description: Raw source data
    schema: dbo
    tables:
      - name: example_table
        description: Example source table
        columns:
          - name: id
            description: Primary key
          - name: name
            description: Example name field
          - name: created_at
            description: Record creation timestamp
